# Copy to the project root as .env.provider-eval. That path is already ignored.
# Existing accounts only. Enter keys locally; never paste keys into chat.
# Deepgram is read from the existing server/.env by the documented commands.
ASSEMBLYAI_API_KEY=
SPEECHMATICS_API_KEY=

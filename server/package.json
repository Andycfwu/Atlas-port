{
  "name": "realtorch-atlas-transcription-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "node --watch --env-file=.env server.mjs",
    "start": "node --env-file=.env server.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.2.1",
    "multer": "2.3.0",
    "openai": "^7.5.0",
    "ws": "^8.21.3"
  }
}
